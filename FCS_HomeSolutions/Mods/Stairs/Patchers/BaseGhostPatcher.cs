using HarmonyLib;

namespace FCS_HomeSolutions.Mods.Stairs.Patchers
{
    //[HarmonyPatch(typeof(BaseGhost), nameof(BaseGhost.Place))]

    //internal class BaseGhostPatcher
    //{
    //    [HarmonyPrefix]
    //    public static bool Prefix(BaseGhost __instance)
    //    {
    //        LBaseConnector = ((__instance.gameObject.FindChild("BaseCorridorHatch") == null) ? null : new Int3?(__instance.TargetOffset));
    //        return true;
    //    }
    //    public static Int3? LBaseConnector;
    //}
}
